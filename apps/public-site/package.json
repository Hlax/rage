{
  "name": "rge-public-site",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only public export surface for the Research Graph Engine. Renders static JSON snapshots only; never connects to the private local engine.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "preview:static": "npx --yes serve@14.2.4 out -p 3000"
  },
  "dependencies": {
    "next": "^15.3.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "typescript": "^5.6.0"
  }
}
