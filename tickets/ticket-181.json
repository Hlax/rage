{
  "id": "ticket-181",
  "title": "Live staged detect mock-fixture spine (opt-in network)",
  "problem": "ticket-178 proves live ingest through mock build. No opt-in proof that mock-fixture detect-contradictions runs after live build path without live LLM.",
  "evidence": [
    "agent_reports/2026-06-15_pre-ticket-180_live-staged-detect-mock-spine-audit.md",
    "tests/unit/test_live_staged_build_mock_spine.py",
    "tests/unit/test_staged_ingest_contradiction_spine.py"
  ],
  "affected_modules": [
    "tests/unit/test_live_staged_detect_mock_spine.py"
  ],
  "expected_files": [
    "tests/unit/test_live_staged_detect_mock_spine.py",
    "agent_reports/2026-06-15_ticket-181_live-staged-detect-mock-spine.md"
  ],
  "acceptance_criteria": [
    "Opt-in live_network test runs discover through detect-contradictions --fixture on temp DB.",
    "Domain opposing-context seed before live network (local mock only).",
    "Mock fixtures only; no live LLM.",
    "Default pytest excludes live_network proof.",
    "Golden tests pass.",
    "Safety audit passes."
  ],
  "test_plan": [
    "python -m pytest tests/unit/test_live_staged_detect_mock_spine.py -q",
    "python -m pytest tests/unit/test_live_staged_build_mock_spine.py -q",
    "python -m pytest tests/golden -q",
    "python -m pytest -q",
    "python -m rge.modules.safety_auditor --audit full"
  ],
  "non_goals": [
    "Live LLM.",
    "reconcile-scores/report.",
    "Public export/site.",
    "Schema migrations.",
    "CI live network by default."
  ],
  "risk_level": "medium",
  "rollback_plan": "Remove live detect mock spine test.",
  "status": "done",
  "category": "Phase 3 / source acquisition / product-risk reduction",
  "pre_ticket_audit_required": true
}
