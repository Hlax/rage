{
  "id": "ticket-184",
  "title": "Live staged reconcile-scores spine (opt-in network)",
  "problem": "ticket-181 proves live ingest through mock detect. No opt-in proof that deterministic reconcile-scores runs after live detect path.",
  "evidence": [
    "agent_reports/2026-06-15_pre-ticket-183_live-staged-reconcile-mock-spine-audit.md",
    "tests/unit/test_live_staged_detect_mock_spine.py",
    "tests/unit/test_staged_ingest_reconcile_spine.py"
  ],
  "affected_modules": [
    "tests/unit/test_live_staged_reconcile_mock_spine.py"
  ],
  "expected_files": [
    "tests/unit/test_live_staged_reconcile_mock_spine.py",
    "agent_reports/2026-06-15_ticket-184_live-staged-reconcile-mock-spine.md"
  ],
  "acceptance_criteria": [
    "Opt-in live_network test runs discover through reconcile-scores on temp DB.",
    "Domain opposing-context seed before live network (local mock only).",
    "Mock LLM fixtures through detect; deterministic reconcile (no live LLM).",
    "Default pytest excludes live_network proof.",
    "Golden tests pass.",
    "Safety audit passes."
  ],
  "test_plan": [
    "python -m pytest tests/unit/test_live_staged_reconcile_mock_spine.py -q",
    "python -m pytest tests/unit/test_live_staged_detect_mock_spine.py -q",
    "python -m pytest tests/golden -q",
    "python -m pytest -q",
    "python -m rge.modules.safety_auditor --audit full"
  ],
  "non_goals": [
    "Live LLM.",
    "report generation.",
    "Public export/site.",
    "Schema migrations.",
    "CI live network by default."
  ],
  "risk_level": "medium",
  "rollback_plan": "Remove live reconcile mock spine test.",
  "status": "done",
  "category": "Phase 3 / source acquisition / product-risk reduction",
  "pre_ticket_audit_required": true
}
