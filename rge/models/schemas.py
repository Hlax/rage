"""Core entity schemas (sources, chunks, claims, concepts, relationships).

Phase 0 stub. Later tickets add typed models mirroring
``docs/agents/05_DATA_MODEL.md``: stable prefixed TEXT IDs, claim lifecycle
(draft/staged/accepted/rejected with reasons), quote-span provenance, and
append-only score events. Domain-specific fields live in ``domain_metadata``
JSON, never as core columns or core model fields.
"""
