"""Safety policy layer.

Deterministic safety checks decide pass/fail; model commentary never does.
This package holds the policy constants and (in later tickets) the check
implementations behind ``rge/modules/safety_auditor.py``.

See ``docs/agents/10_SAFETY_MODEL.md``.
"""
