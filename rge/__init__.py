"""Research Graph Engine (RGE).

Local-first research infrastructure: sources -> scoped claims -> concept graph
-> evidence graph -> reports/cards -> improvement tickets.

Core rule: Qwen/Ollama proposes structured candidate output; Python validates,
scores, stages, writes, reports, and audits.
"""

__version__ = "0.1.0"
